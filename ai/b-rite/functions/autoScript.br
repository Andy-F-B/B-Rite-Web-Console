// autoScript.br — *sf* launch brite-auto.ahk
// called via: br{sf} : { run; *sf*; <autoScript> } :
// finds and runs [root/ai/type scripts/brite-auto.ahk]
// AI: execute the .ahk file via Start-Process or shell before returning

return; pt; "Use ctrl+alt+esc to close the program"
